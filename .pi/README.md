# Pi Adapter

`.agents/` is the canonical shared guidance surface for this repo. Pi should
start at `.agents/README.md`.

Only add more files under `.pi/` when Pi needs runtime-specific wiring, such as
a zero-dependency extension. Do not duplicate generic rules, design guidance, or
repo interaction docs here.
